document.querySelectorAll('.btn').forEach(b=>b.addEventListener('mouseenter',()=>b.style.filter='brightness(1.1)'));
document.querySelectorAll('.btn').forEach(b=>b.addEventListener('mouseleave',()=>b.style.filter='brightness(1)'));
